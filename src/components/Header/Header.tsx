import { ReactComponent as LogoIcon } from './images/logo.svg'

import './Header.scss'

const Header = () => {
  return (
    <header className='header'>
      <LogoIcon />
    </header>
  )
}

export default Header
